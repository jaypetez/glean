name: glean-ex06

services:
  glean:
    image: ghcr.io/jaypetez/glean:latest
    container_name: glean-ex06-glean
    init: true
    stop_grace_period: 30s
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    depends_on:
      ollama:
        condition: service_healthy
      mailpit:
        condition: service_started
    env_file:
      - .env
    environment:
      TZ: ${TZ:-UTC}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    volumes:
      - ./data:/data
      - ./feeds.yaml:/etc/glean/feeds.yaml:ro
    networks:
      - glean-ex06
    ports:
      - "127.0.0.1:9096:9090"

  ollama:
    image: ollama/ollama:latest
    container_name: glean-ex06-ollama
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=64m,mode=1777
    volumes:
      - ./data/ollama:/root/.ollama
    networks:
      - glean-ex06
    healthcheck:
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  mailpit:
    image: axllent/mailpit:latest
    container_name: glean-ex06-mailpit
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - glean-ex06
    ports:
      - "127.0.0.1:8025:8025"

networks:
  glean-ex06:
    name: glean-ex06
    driver: bridge
